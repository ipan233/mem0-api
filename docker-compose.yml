version："3.9"

services:
  mem0-api:
    build: .
    networks: [bridge]
    ports: ["9001:9001"]
    env_file: .env
    depends_on: [qdrant]

  qdrant:
    image: qdrant/qdrant:v1.8.3
    networks: [bridge]
    ports: ["6333:6333"]
    volumes: [qdrant_data:/qdrant/storage]

networks:
  bridge:
    external: true
    name: openclaw

volumes:
  qdrant_data:
